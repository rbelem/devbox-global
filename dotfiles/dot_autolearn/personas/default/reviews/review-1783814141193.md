# Autolearn Review

## Context

- Project: devbox-global
- Date: 2026-07-11T23:55:41.193Z
- Turns in this review: 2
- Trigger: exit

## Instructions

Review the conversation below for learning opportunities.
Load the autolearn-reviewer skill with: skill({ name: "autolearn-reviewer" })

Focus on:

1. User corrections (style, approach, tools) — "don't do X", "use Y instead"
2. User preferences AND declarative workflow specs — not just corrections.
   The user may describe how they want something done without a mistake
   being made first. Capture these too:
   - "they should be one post one week" (cadence spec)
   - "we don't use global pip anywhere here" (system-wide tool rule)
   - "LinkedIn should follow Bluesky schedule" (sync rule)
   - "use PEP 723 inline metadata for Python scripts" (convention)
3. Workarounds or techniques that worked
4. Skills that were wrong, incomplete, or outdated
5. Repeated patterns worth capturing

IMPORTANT: Preferences are not always corrections. Scan every user message
for declarative specs ("should be", "we use", "we don't", "I want") even
when no error occurred. Record general rules, not narrow instances.

## Conversation

### Assistant

The user is asking why `devbox global run update-flake` is not displaying versions correctly. Let me look at the AGENTS.md context I have — there's actually a documented pitfall about this:

> **Pitfall**: `devbox global run update-flake` uses the current shellenv which may not have `gh` in PATH. If it shows all packages as "unknown", run `eval "$(devbox global shellenv --recompute)"` first, then run the script directly from `$(devbox global path)/bin/update-flake`.

So the answer is documented. But let me verify this is actually the issue by checking the current state. The user said "not displaying the versions correctly" which matches the symptom described — packages show as "unknown".

Let me think about what to do here. This is a diagnostic question. I could:
1. Just answer from the documented pitfall (fast, but assumes it's the same issue)
2. Actually investigate the current state to confirm

Per my role, I should be retrieval-led, not pretrained-led. The doc gives a strong hint, but I should verify rather than assume. Let me actually run the command to see what's happening, then check if `gh` is in the shellenv.

Actually, let me be efficient. The documented pitfall is very specific: the script needs `gh` to query the GitHub API, and `devbox global run` uses the current shellenv which may not have `gh`. The fix is to recompute the shellenv first.

But I shouldn't just assume — let me verify the current state. I'll delegate to @fast-generic to run diagnostic commands: check if `gh` is available, run the update-flake script, and capture the output. This is routine mechanical command work.

Actually, let me think about this more carefully. The user wants to know WHY. I have a documented answer but should verify it applies to the current situation. Let me run a few diagnostic commands in parallel via @fast-generic:
1. Check if `gh` is in PATH
2. Run the update-flake script and capture output
3. Check `devbox global shellenv` for gh

Let me dispatch this to fast...

### Assistant

The diagnosis is clear. The documented hypothesis in the AGENTS.md was wrong — `gh` IS in PATH. The real issue is that `gh` is not authenticated. Let me give the user a concise answer.The documented hypothesis is wrong. `gh` IS in PATH in all contexts — both `devbox global run update-flake` and running the script directly produce identical "unknown" output.

**Real cause: `gh` is not authenticated.**

`gh auth status` reports `You are not logged into any GitHub hosts.` and neither `GH_TOKEN` nor `GITHUB_TOKEN` is set. The script calls `gh release list`, `gh api repos/.../tags`, and `gh api repos/...` — all three fail with exit 4, but the script redirects stderr to `/dev/null` with `|| true`, so the auth error is swallowed silently and every package falls through to `LATEST="unknown"`.

**Fix — pick one:**
- `gh auth login` (interactive OAuth)
- Set `GH_TOKEN` env var (e.g. a GitHub personal access token)

I'll update the AGENTS.md note since the documented cause is incorrect. Want me to also add an auth check to the script so it prints a clear warning instead of silent "unknown" rows?

---

Take action now.
